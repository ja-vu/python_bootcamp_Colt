# abs
# return the absolute value of a number. The argument may be an integer or a floating point number

# The magnitude of a real number without regard to its sign
abs(-3.5535)
abs(-23)

# sum
# Takes an iterable and an optional start
# Returns the sum of start and the items of an iterable from left to right and returns the total
# Starts at 0
sum([1,2,3])
sum([1,2,3], 10)
sum([1,2,3], -6)
sum([1.5,2.3,3.7])
sum(['hi','there'])

''.join(['hi','there'])

# round
# returns the number rounded to n digits precision after the decimal point. If ndigits is omitted or is None
# it returns the nerest integer to ints input
round(10.2)
round(1.5)
round(3.512345)
round(3.512345, 3)
round(3.512345, None)